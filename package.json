{
  "name": "simon-says-leaderboard",
  "version": "1.0.0",
  "description": "IoT Leaderboard System for Simon Says Game",
  "main": "server.js",
  "scripts": {
    "start": "node startup.js",
    "dev": "nodemon startup.js",
    "server": "node server.js",
    "test": "node test.js",
    "setup": "npm install && node -e \"require('fs').writeFileSync('leaderboard.json', '[]')\"",
    "check": "node -e \"console.log('✅ Setup complete - Simon Says IoT ready!')\"",
    "lint": "echo 'No linter configured - add ESLint for better code quality'",
    "clean": "node -e \"require('fs').writeFileSync('leaderboard.json', '[]'); console.log('🧹 Leaderboard cleared')\"",
    "backup": "node -e \"const fs=require('fs'); const data=fs.readFileSync('leaderboard.json'); fs.writeFileSync('leaderboard.backup.json', data); console.log('💾 Backup created')\"",
    "azure-deploy": "powershell -ExecutionPolicy Bypass -File ./deploy-to-azure.ps1",
    "health-check": "curl http://localhost:3000/health || echo 'Server not running'"
  },
  "keywords": [
    "iot",
    "leaderboard",
    "simon-says",
    "esp8266",
    "arduino",
    "express"
  ],
  "author": "IoT Project Team",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.10.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
